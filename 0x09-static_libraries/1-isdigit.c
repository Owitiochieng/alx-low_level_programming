#include "main.h"
/**
 * _isdigit - Looks for digit i.e, from 0 to 9 in entered integers
 * @c: The integer input
 * Return: 1 if input is a digit else return 0
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
