#include "main.h"
/**
 * _abs - A function that returns the absolute value of an integer
 * @n: the integer input
 * Return: the result
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
